from database import db_connection
from database import insert_data

if __name__ == "__main__":
    # db에 csv 값 입력 완료, 재사용 기능x(주석 풀지 마세요)
    # insert_data.h2_FAQ("data\processed\h2_faq.csv") # db 생성완료
    # insert_data.annual_H2_ev_registrations("data\processed\\annual_h2_ev_registrations.csv") # db 생성 완료
    # insert_data.h2_station_info("data\processed\h2_station_info.csv") # db 생성 완료
    # insert_data.h2_stations_by_region("data\processed\h2_stations_by_region.csv") # db 생성 완료
    # insert_data.ev_stations_by_region("data\processed\ev_stations_by_region.csv") # db 생성 완료
    pass